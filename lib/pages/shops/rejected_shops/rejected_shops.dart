import 'package:flutter/material.dart';

class RejectedShopsScreen extends StatelessWidget {
  const RejectedShopsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
